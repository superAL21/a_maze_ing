[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "mazegen"
version = "1.0.0"
description = "A maze generator with perfect and Pac-Man modes"
requires-python = ">=3.10"

[tool.setuptools]
py-modules = ["maze_generator", "cell"]